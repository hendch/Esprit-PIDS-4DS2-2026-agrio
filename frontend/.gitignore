# Ignores paths under frontend/ only. Run Expo from this directory (`cd frontend`).
# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# Dependencies
node_modules/

# Logs
*.log

# Expo (also covers mistaken `.expo/` only here, not at repo root)
.expo/
dist/
web-build/
expo-env.d.ts

# Native build / secrets
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
# Prebuild output at this folder level (equivalent to root-level `android/` / `ios/` for this app)
/android
/ios

# Metro
.metro-health-check*

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# Env
.env*.local

# TypeScript
*.tsbuildinfo
